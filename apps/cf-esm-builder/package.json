{
    "name": "@onlook/cf-esm-builder",
    "private": true,
    "scripts": {
        "test": "bun run scripts/run-tests-isolated.ts",
        "typecheck": "tsc --noEmit",
        "dev": "wrangler dev",
        "deploy": "wrangler deploy"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "latest",
        "typescript": "latest",
        "wrangler": "latest"
    }
}
