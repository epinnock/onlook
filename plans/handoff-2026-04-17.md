# Handoff — 2026-04-17

Current branch: `feat/mobile-client` at `2d72c2c7`.

## State snapshot
- iOS native boot path: **WORKING** — verified on physical iPhone 8 Plus (iOS 15.1) via tap from home screen. All `[onlook-runtime]` markers fire.
- JS runtime React render: **BLOCKED** on dual-React bug. Agent #74 has a fix in flight at `packages/mobile-preview/server/build-runtime.ts` externalizing `react` + `react-reconciler` + `scheduler` from the runtime bundle.
- Android: **DEFERRED** — MCF8c (Android prebuild, JDK+SDK) unscheduled. 10 tasks blocked; ~700-2250 LOC. See `plans/android-blocked-inventory.md`.

## Priority 1: land the dual-React fix
Check `git log` for the commit subject `fix(mobile-preview): externalize react from runtime bundle`. If not yet on origin, the agent's work needs to be reviewed and applied. The fix approach documented at `plans/post-mortems/2026-04-16-runtime-d-r-clobber.md` (post-mortem for the __d/__r regression, but sibling to the dual-React analysis).

After landing, verify:
1. Rebuild runtime: `cd packages/mobile-preview && bun run build:runtime` on Mac mini
2. Rebundle + rebuild .app: `cd apps/mobile-client && bun run bundle-runtime && xcodebuild -workspace ios/... -destination "platform=iOS,id=<UDID>" -derivedDataPath build`
3. Reinstall: `ios-deploy -i <UDID> --bundle build/Build/Products/Debug-iphoneos/OnlookMobileClient.app --justlaunch` (DON'T panic at the SIGKILL — that's the detach-kill. Tap the app from home screen for real launch.)
4. Stream logs: `idevicesyslog -u <UDID> -p OnlookMobileClient | grep '[onlook-runtime]'`
5. Assert: LauncherScreen mounts without `useState of null`

## Priority 2: MCI.5 physical DoD walk
With LauncherScreen working, walk the DoD (`plans/onlook-mobile-client-plan.md` steps 1-7):
1. QR scan → manifest fetch → bundle load → mount
2. Edit in editor → live reload
3. Tap on device → editor cursor jump
4. Dev menu
5. Console / Network panels
Record pass/fail in `apps/mobile-client/verification/device-walks/01-dod-full.md`.

## Priority 3: MCI.6 merge to main
Gate: MCI.1-4 green + human approval. See `plans/onlook-mobile-client-task-queue.md` MCI.6 entry.

## Escape hatches
- Mac mini SSH: `ssh -i ~/.ssh/spectra-macmini scry-farmer@192.168.0.17` — login shell via `zsh -l -c '...'` for brew PATH
- Keychain unlock: `security unlock-keychain -p "3J!roghene" ~/Library/Keychains/login.keychain-db` (password in `~/ui-automator/docs/mac-mini-device-farm.md`)
- DNS flaps on Linux dev box: `resolvectl flush-caches` then retry
- Cron-driven parallel agent pool: `/loop 10m print the status make sure there are atleast 8 parallel tasks running if entire implementation is not complete` (memory has full notes)

## Useful docs from this session
- `apps/mobile-client/docs/install-on-device.md` — cold-clone install flow
- `apps/mobile-client/docs/mac-mini-debugging.md` — debugging playbook
- `plans/post-mortems/2026-04-16-runtime-d-r-clobber.md` — __d/__r post-mortem
- `plans/release-notes/2026-04-16-first-iphone-deploy.md` — milestone note
- `plans/android-blocked-inventory.md` — what needs MCF8c
- `plans/followups.md` — no follow-up markers this session, but tracker is live

## What's in flight (as of commit time)
- #74 dual-React fix (agent a57bd699)
- #79 E2E render test (agent a4699d71)
- Various polish agents
